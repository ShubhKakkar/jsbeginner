const sluggify = (text,lower) => {
    if(lower){
        return text.toLowerCase();
    }
    else{
        return text;
    }
}

sluggify("I am Shubham Kakkar",true);

consoe.log(typeof(1000));
console.log(typeof("name"));

console.log(10==10);
if(name == "scott" || (name == "wes" && last == "bos")){
    console.log("Cool Name");
}
const sluggify = (text,lower) => {
    if(lower){
        return text.toLowerCase();
    }
    else{
        return text;
    }
}
console.log(true || true || false || false);
console.log(true && false && false && true);
Object.keys({name:"wes"}).length
//This will return 1 as the answer as the Object is only having one key value name:"wes".
const object1 = {
    a: 'somestring',
    b: 42,
    c: false
  };
  
  console.log(Object.keys(object1));
  // expected output: Array ["a", "b", "c"]


const object1 = {
    a: 'somestring',
    b: 42,
    c: false
  };
  
  console.log(Object.keys(object1));
  // expected output: Array ["a", "b", "c"]

